// example function:
function multiply(num1, num2) {
  return num1 * num2
}

function addNumbers(num1, num2) {
  return num1 + num2
}

function divide(a, b) {
  if (b === 0) {
    return "Cannot divide by zero"
  } else {
    return a / b
  }
}
